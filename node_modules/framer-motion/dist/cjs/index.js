import React from 'react';
import './Header.css';
import logo from '../../assets/logo-leilao.png';
// 1. Importe os ícones que vamos usar
import { FaBars, FaUserCircle } from 'react-icons/fa';

const Header = () => {
    // Você não está usando props, então pode deixar o parâmetro vazio
    return (
        // A tag <> e </> (Fragment) não é necessária se você só tem um elemento principal
        <div className="header">
            {/* 2. Crie as três seções do cabeçalho */}

            {/* Esquerda: Ícone de Menu */}
            <div className="header-left">
                <FaBars className="header-icon" />
            </div>

            {/* Centro: Logo */}
            <div className="header-center">
                <img className="logo" src={logo} alt="Logo do site AgroLance" />
            </div>

            {/* Direita: Ícone de Usuário */}
            <div className="header-right">
                <FaUserCircle className="header-icon" />
            </div>
        </div>
    );
}

export default Header;